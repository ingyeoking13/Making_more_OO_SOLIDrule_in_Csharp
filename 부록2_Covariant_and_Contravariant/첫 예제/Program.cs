namespace 부록2_Covariant_and_Contravariant
{
    class Program
    {
        //static void Main(string[] args) {
            /*
            var integers = new ArrayList();
            integers.Add(1);
            integers.Add("1");
            integers.Add("kimchi");
            foreach( var i in integers) Console.WriteLine(i);
            //foreach( int i in integers) Console.WriteLine(i);

            var T_integers = new MyQueue<int>();
            T_integers.Add(1);
            T_integers.Add(5);
            T_integers.Add(11);
            foreach( int i in T_integers) Console.WriteLine(i);


            var PersonQueue = new MyQueue<Person>();
            PersonQueue.Add(new Student("Kimchi"));

            var StudentQueue = new MyQueue<Student>();
            PersonQueue = StudentQueue;
            */

            /*
            QueueInterface<Student> students = new MyQueue<Student>();
            QueueInterface<Person> persons = students;
            */
//        }
    }
}
